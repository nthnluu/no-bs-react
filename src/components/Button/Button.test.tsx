import Button from "./Button";
import {render} from "@testing-library/react";

describe('isValidEmail', () => {
    it('should ', function () {
        const but = render(<Button/>);
        expect(12).toBe(12);
    });
});